<%- include('partials/header') %>

<main>
  <section class="hero-section hero-section-bg">
    <div class="parallax-bg"></div>
    <canvas id="starCanvas"></canvas>
    <div class="overlay"></div>
    <div class="hero-content container text-center text-white position-relative">
      <h1 class="display-4 fw-bold mb-3">Impulsa tu Futuro en Tecnología</h1>
      <p class="lead mb-4">Forma parte de una generación de profesionales preparados para liderar la transformación digital.</p>
      <a href="/programa" class="btn btn-primary-custom btn-lg rounded-pill">Conoce el Programa</a>
    </div>
  </section>

  <section class="py-5 bg-light-custom">
    <div class="container">
      <h2 class="section-title">¿Por qué estudiar con nosotros?</h2>
      <div class="row g-4 mt-4">
        <div class="col-md-4 fade-in-on-scroll">
          <div class="card h-100 text-center p-4 border-0 shadow-sm">
            <i class="bi bi-cpu feature-icon"></i>
            <h5 class="card-title mt-3">Tecnología Avanzada</h5>
            <p class="card-text">Acceso a laboratorios, plataformas cloud y herramientas de desarrollo modernas.</p>
          </div>
        </div>
        <div class="col-md-4 fade-in-on-scroll">
          <div class="card h-100 text-center p-4 border-0 shadow-sm">
            <i class="bi bi-diagram-3 feature-icon"></i>
            <h5 class="card-title mt-3">Pensamiento Crítico</h5>
            <p class="card-text">Formación basada en la resolución de problemas reales y toma de decisiones estratégicas.</p>
          </div>
        </div>
        <div class="col-md-4 fade-in-on-scroll">
          <div class="card h-100 text-center p-4 border-0 shadow-sm">
            <i class="bi bi-code-slash feature-icon"></i>
            <h5 class="card-title mt-3">Proyección Profesional</h5>
            <p class="card-text">Alta empleabilidad en empresas líderes del sector TI y startups innovadoras.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-5 bg-light-custom">
    <div class="container">
      <h2 class="section-title">Nuestros Números</h2>
      <div class="row g-4 mt-4 text-center">
        <div class="col-md-3">
          <div class="counter-box">
            <i class="bi bi-people-fill counter-icon"></i>
            <h3 class="counter" data-target="1200">0</h3>
            <p>Estudiantes</p>
          </div>
        </div>
        <div class="col-md-3">
          <div class="counter-box">
            <i class="bi bi-mortarboard-fill counter-icon"></i>
            <h3 class="counter" data-target="5000">0</h3>
            <p>Graduados</p>
          </div>
        </div>
        <div class="col-md-3">
          <div class="counter-box">
            <i class="bi bi-journal-code counter-icon"></i>
            <h3 class="counter" data-target="300">0</h3>
            <p>Publicaciones</p>
          </div>
        </div>
        <div class="col-md-3">
          <div class="counter-box">
            <i class="bi bi-award-fill counter-icon"></i>
            <h3 class="counter" data-target="50">0</h3>
            <p>Premios</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-5">
    <div class="container">
      <h2 class="section-title">Lo que dicen nuestros estudiantes</h2>
      <div id="testimonialCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <div class="testimonial-card">
              <i class="bi bi-person-circle testimonial-icon"></i>
              <p class="testimonial-text">"La formación que recibí aquí me abrió las puertas a oportunidades que nunca imaginé. Los profesores son increíbles y el ambiente es muy motivador."</p>
              <p class="testimonial-author">- Ana Sofía</p>
            </div>
          </div>
          <div class="carousel-item">
            <div class="testimonial-card">
              <i class="bi bi-person-fill testimonial-icon"></i>
              <p class="testimonial-text">"Gracias a los proyectos prácticos, pude aplicar mis conocimientos y desarrollar un portafolio sólido que me ayudó a conseguir mi primer trabajo antes de graduarme."</p>
              <p class="testimonial-author">- Carlos Alberto</p>
            </div>
          </div>
          <div class="carousel-item">
            <div class="testimonial-card">
              <i class="bi bi-person testimonial-icon"></i>
              <p class="testimonial-text">"La calidad de la enseñanza y el acceso a tecnología de punta son inigualables. Recomiendo esta facultad a cualquiera que quiera destacar en el mundo de la tecnología."</p>
              <p class="testimonial-author">- María Fernanda</p>
            </div>
          </div>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#testimonialCarousel" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#testimonialCarousel" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>
    </div>
  </section>

  <section class="cta-section text-white cta-section-bg">
    <div class="overlay"></div>
    <div class="container text-center position-relative">
      <h2 class="mb-4">¿Estás listo para comenzar tu camino en la ingeniería de sistemas?</h2>
      <p class="lead">Conviértete en el profesional que el mundo digital necesita. Te estamos esperando.</p>
      <a href="/contacto" class="btn btn-secondary-custom btn-lg rounded-pill mt-3">Contáctanos Ahora</a>
    </div>
  </section>
<main>

<script>
  const canvas = document.getElementById('starCanvas');
  const ctx = canvas.getContext('2d');

  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;

  let stars = [];

  function addStar() {
    stars.push({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      radius: Math.random() * 1.5,
      vx: (Math.random() - 0.5) * 0.5,
      vy: (Math.random() - 0.5) * 0.5
    });
  }

  function update() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);

    ctx.fillStyle = "#fff";
    for (let i = 0, x = stars.length; i < x; i++) {
      let s = stars[i];
      ctx.beginPath();
      ctx.arc(s.x, s.y, s.radius, 0, 2 * Math.PI);
      ctx.fill();
    }
  }

  function animate() {
    for (let i in stars) {
      let s = stars[i];

      s.x += s.vx;
      s.y += s.vy;

      if (s.x < 0 || s.x > canvas.width) s.x = -s.x;
      if (s.y < 0 || s.y > canvas.height) s.y = -s.y;
    }

    update();
    requestAnimationFrame(animate);
  }

  for (let i = 0; i < 80; i++) {
    addStar();
  }

  const heroSection = document.querySelector('.hero-section');
  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        animate();
      } else {
      }
    });
  }, { threshold: 0.1 });

  observer.observe(heroSection);

  const parallaxBg = document.querySelector('.parallax-bg');
  let ticking = false;

  window.addEventListener('scroll', function() {
    if (!ticking) {
      window.requestAnimationFrame(function() {
        const offset = window.pageYOffset;
        parallaxBg.style.transform = `translateY(${offset * 0.5}px)`;
        ticking = false;
      });

      ticking = true;
    }
  });

  const counters = document.querySelectorAll('.counter');

  const animateCounters = () => {
    counters.forEach(counter => {
      const target = +counter.getAttribute('data-target');
      let count = 0;

      const updateCount = () => {
        const increment = target / 100;
        if (count < target) {
          count += increment;
          counter.innerText = Math.ceil(count);
          requestAnimationFrame(updateCount);
        } else {
          counter.innerText = target;
        }
      };

      updateCount();
    });
  };

  const numbersObserver = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        animateCounters();
        numbersObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  const numbersSection = document.querySelector('.bg-light-custom');
  numbersObserver.observe(numbersSection);
</script>

<%- include('partials/footer') %>clude('partials/footer') %>